import React from 'react';
import SideBar from './video-sidebar';
import { shallow } from 'enzyme'
import toJson from 'enzyme-to-json'
import { mapStateToProps } from './sidebar-container'

describe('Should render video-sidebar', () => {

})

describe('mapStateToProps works correctly', () => {

})

describe('RenderCards is called with correct props', () => {
  
})
